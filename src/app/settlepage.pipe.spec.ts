import { SettlepagePipe } from './settlepage.pipe';

describe('SettlepagePipe', () => {
  it('create an instance', () => {
    const pipe = new SettlepagePipe();
    expect(pipe).toBeTruthy();
  });
});
